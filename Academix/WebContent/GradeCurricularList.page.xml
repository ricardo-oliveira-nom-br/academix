<?xml version="1.0" encoding="UTF-8"?>
<page xmlns="http://jboss.com/products/seam/pages"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://jboss.com/products/seam/pages http://jboss.com/products/seam/pages-2.0.xsd">

	<param name="firstResult"
		value="#{gradeCurricularList.firstResult}" />
	<param name="order" value="#{gradeCurricularList.order}" />
	<param name="from" />
	<param name="curso"
		value="#{gradeCurricularList.gradeCurricular.curso}" />
	<param name="numAno"
		value="#{gradeCurricularList.gradeCurricular.numAno}" />
	<param name="numSemestre"
		value="#{gradeCurricularList.gradeCurricular.numSemestre}" />

	<restrict>
		#{identity.loggedIn and (s:hasRole("admin") or
		s:hasRole("secretaria"))}
	</restrict>

</page>
